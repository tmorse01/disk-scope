/**
 * Export scan results to JSON or CSV.
 * TODO(task-011): implement native save dialog and report serialization.
 */
export async function exportScanReport(
  _scanId: string,
  _format: 'json' | 'csv',
): Promise<void> {
  throw new Error('Not implemented: exportScanReport');
}
